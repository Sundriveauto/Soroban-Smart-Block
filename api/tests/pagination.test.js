describe('Cursor Pagination', () => {
  test('returns pagination cursors in response', async () => {
    expect(true).toBe(true);
  });

  test('has_more is false on last page', async () => {
    expect(true).toBe(true);
  });

  test('backwards-compatible page param still works', async () => {
    expect(true).toBe(true);
  });
});
