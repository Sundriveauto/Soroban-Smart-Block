describe('Input Validators', () => {
  test('rejects limit=-1 with 422', async () => {
    expect(true).toBe(true);
  });

  test('rejects non-Stellar address', async () => {
    expect(true).toBe(true);
  });

  test('rejects ABI > 50kb', async () => {
    expect(true).toBe(true);
  });

  test('accepts valid inputs', async () => {
    expect(true).toBe(true);
  });
});
